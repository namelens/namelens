{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://crucible.3leaps.dev/schemas/ailink/v0/prompt",
  "title": "AILink Prompt Configuration",
  "description": "Schema for AILink prompt configuration files",
  "type": "object",
  "required": [
    "slug",
    "system_template"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*[a-z0-9]$|^[a-z0-9]$",
      "minLength": 1,
      "maxLength": 64,
      "description": "Unique identifier for the prompt (lowercase alphanumeric with hyphens)"
    },
    "name": {
      "type": "string",
      "maxLength": 128,
      "description": "Human-readable display name"
    },
    "description": {
      "type": "string",
      "maxLength": 512,
      "description": "Brief description of what this prompt analyzes"
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Semantic version for prompt content evolution"
    },
    "author": {
      "type": "string",
      "maxLength": 64,
      "description": "Prompt author or maintainer"
    },
    "updated": {
      "type": "string",
      "format": "date",
      "description": "Last update date (YYYY-MM-DD)"
    },
    "input": {
      "type": "object",
      "description": "Input specification",
      "properties": {
        "required_variables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Variables that must be provided (e.g., ['name'])"
        },
        "optional_variables": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional variables with defaults"
        },
        "accepts_images": {
          "type": "boolean",
          "default": false,
          "description": "Whether this prompt accepts image input"
        },
        "image_types": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "image/png",
              "image/jpeg",
              "image/webp",
              "image/gif"
            ]
          },
          "description": "Accepted image MIME types (if accepts_images is true)"
        },
        "max_images": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20,
          "default": 1,
          "description": "Maximum number of images allowed"
        }
      }
    },
    "system_template": {
      "type": "string",
      "minLength": 1,
      "description": "System prompt template with {{variable}} placeholders"
    },
    "user_template": {
      "type": "string",
      "default": "{{input}}",
      "description": "User prompt template (optional)"
    },
    "depth_variants": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Keyed user prompt variants (e.g., quick, deep)"
    },
    "tools": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "type"
        ],
        "properties": {
          "type": {
            "type": "string",
            "description": "Tool type identifier (e.g., web_search, x_search)"
          },
          "config": {
            "type": "object",
            "description": "Tool-specific configuration"
          }
        }
      },
      "description": "Server-side tools to request"
    },
    "response_schema": {
      "anyOf": [
        {
          "type": "object",
          "description": "Inline JSONSchema for response validation"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "description": "Reference to external schema"
            }
          }
        }
      ],
      "description": "JSONSchema for expected response structure"
    },
    "response_options": {
      "type": "object",
      "properties": {
        "may_include_images": {
          "type": "boolean",
          "default": false,
          "description": "Whether response may contain images"
        },
        "image_types": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Expected image MIME types in response"
        }
      }
    },
    "provider_hints": {
      "type": "object",
      "properties": {
        "preferred_models": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Models known to work well with this prompt"
        },
        "min_context_window": {
          "type": "integer",
          "description": "Minimum context window size needed"
        },
        "supports_tools": {
          "type": "boolean",
          "default": true,
          "description": "Whether this prompt benefits from tool use"
        }
      }
    }
  },
  "additionalProperties": false
}
